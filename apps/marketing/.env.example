# CONTEXT: local dev of marketing only. Copy to .env.local (all public, build-time).

# Funnel destinations. CTAs route here. apps/app does not exist yet, so signup CTAs
# point at the demo for now (re-point NEXT_PUBLIC_APP_URL when the dashboard ships).
NEXT_PUBLIC_DEMO_URL=https://bankstract.logickoder.dev/demo
NEXT_PUBLIC_DOCS_URL=https://bankstract.logickoder.dev/docs
NEXT_PUBLIC_APP_URL=https://bankstract.logickoder.dev/demo
